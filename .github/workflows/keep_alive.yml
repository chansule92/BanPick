name: Keep Render Server Awake

on:
  schedule:
    - cron: '*/13 * * * *'
  workflow_dispatch: # 수동 실행 옵션

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Website
        run: curl https://banpick.onrender.com/
